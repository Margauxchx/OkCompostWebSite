<%= form_with(model: result, local: true, html: { class: 'info-form'}) do |form| %>
  <% if result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(result.errors.count, "error") %> prohibited this result from being saved:</h2>

      <ul>
      <% result.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="jumbotron">
    <div class="container h-100">
      <div class="row" id="jumbotron-row">
        <div class="col-6">
            <h1 id="jumbotron-title" class="display-3 mx-5 text-center">Offrez de belles rencontres à vos épluchures !</h1> 
        </div>
        <div class="col-6">
          <div class="card mx-5">
            <div class="card-body">
              <h5 class="card-title">Cherchez un compost !</h5>
              <h6 class="card-subtitle mb-2 text-muted"> Renseignez vos critères de sélection :</h6>
              
              <div>
                <%= select_tag(:district, options_for_select([['001', '001'], ['002', (format '%03d', 002)], ['003', 003], ['004', 004], ['005', 005], ['006', 006], ['007', 007], ['008', '008'], ['009', '009'], ['010', 010], ['011', 011], ['012', 012], ['013', 013], ['014', 014], ['015', 015], ['016', 016], ['017', 017], ['018', '018'], ['019', '019']]), class: "form-control-welcome") %>
              </div>

              <div class="field">
                <%= form.label :bio, class: "mr-2" %>
                <%= form.check_box(:composition, {:multiple => true}, "bio", nil) %>
              </div>

              <div class="field">
                <%= form.label :coquilles, class: "mr-2" %>
                <%= form.check_box(:composition, {:multiple => true}, "coquilles", nil) %>
              </div>

              <div class="field">
                <%= form.label :bananes, class: "mr-2" %>
                <%= form.check_box(:composition, {:multiple => true}, "bananes", nil) %>
              </div>
              <button class="btn btn-primary">Et zou !</button>
            </div>
          </div>  
        </div>
      </div>
    </div>
  </div>

<% end %>
