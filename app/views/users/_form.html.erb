<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row border">
  <div class="col-sm-6 offset-sm-3 border">
    <div class="field">
      <%= form.label :firstname %>
      <%= form.text_field :firstname, class: "form-control", placeholder:raw("&#xF406;  Prénom"), style: "font-family: 'FontAwesome';" %>
    </div>

    <div class="field">
      <%= form.label :lastname %>
      <%= form.text_field :lastname, class: "form-control", placeholder:raw("&#xf0E0;  Email"), style: "font-family: 'FontAwesome';" %>
    </div>

    <div class="field">
      <%= form.label :username %>
      <%= form.text_field :username, class: "form-control", placeholder:raw("&#xf0E0;  Email"), style: "font-family: 'FontAwesome';" %>
    </div>

    <div class="field">
      <%= form.label :email %>
      <%= form.text_field :email, class: "form-control", placeholder:raw("&#xf0E0;  Email"), style: "font-family: 'FontAwesome';" %>
    </div>

    <div class="field">
      <%= form.label :address %>
      <%= form.text_field :address, class: "form-control", placeholder:raw("&#xf0E0;  Email"), style: "font-family: 'FontAwesome';" %>
    </div>

    <div class="field">
      <%= form.label :zipcode %>
      <%= form.text_field :zipcode, class: "form-control", placeholder:raw("&#xf0E0;  Email"), style: "font-family: 'FontAwesome';" %>
    </div>

    <div class="field">
      <%= form.label :city %>
      <%= form.text_field :city, class: "form-control", placeholder:raw("&#xf0E0;  Email"), style: "font-family: 'FontAwesome';" %>
    </div>

    <div class="field">
      <%= form.label :country %>
      <%= form.text_field :country, class: "form-control", placeholder:raw("&#xf0E0;  Email"), style: "font-family: 'FontAwesome';" %>
    </div>

    <div class="field">
      <%= form.label :avatar_url %>
      <%= form.text_field :avatar_url, class: "form-control", placeholder:raw("&#xf0E0;  Email"), style: "font-family: 'FontAwesome';" %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>   
  </div>
</div>

<% end %>
